#Docker containerized Kubernetes Scheduler

## Kubernetes release v0.17.1

To run

```bash
docker run --rm -p XXXX:XXXX --name kube-scheduler l337ch/docker-kube-scheduler /kube-scheduler [runtime options]